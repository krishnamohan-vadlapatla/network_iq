version: '3.8'
services:
  networkiq-optimizer:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - ../data:/app/data
    environment:
      - NIQ_LOG_LEVEL=INFO
